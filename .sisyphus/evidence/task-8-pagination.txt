LIMIT/OFFSET usage in get_sentences_filtered (repository.py lines 305-317):

    params.append(limit)
    params.append(offset)

    cursor = self._conn.execute(
        f"""
        SELECT id, japanese_text, chinese_translation, explanation,
               source_context, created_at
        FROM sentence_records
        {where_sql}
        {order_sql}
        LIMIT ? OFFSET ?
        """,  # noqa: S608
        params,
    )

Both LIMIT and OFFSET use parameterized ? placeholders — no f-string injection.
