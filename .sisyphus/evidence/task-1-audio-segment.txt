Task 1: AudioSegment Dataclass
================================

File Modified: src/db/models.py

Changes:
- Added: import numpy as np (after stdlib imports)
- Added: AudioSegment dataclass after SentenceResult

Ruff Output:
All checks passed!
1 file left unchanged

QA Verification:
$ python -c "import numpy as np; from src.db.models import AudioSegment; seg = AudioSegment(samples=np.zeros(16000, dtype='float32'), duration_sec=1.0); print('OK')"
OK

Commit: feat(models): add AudioSegment dataclass for VAD→ASR handoff
