================================================================================
F2 CODE QUALITY REVIEW — pipeline-perf branch (UPDATED AFTER FIXES)
Date: 2026-03-09
Reviewer: Sisyphus-Junior (claude-sonnet-4.6)
================================================================================

SUMMARY
-------
Lint [PASS] | Format [PASS] | Types [PASS] | Tests [110 pass / 0 fail] (pipeline modules) | VERDICT: APPROVE

================================================================================
SECTION 1 — AUTOMATED TOOL RESULTS
================================================================================

RUFF LINT (ruff check . --exclude .venv)
  Result: PASS — "All checks passed!"
  Exit code: 0

RUFF FORMAT (ruff format --check . --exclude .venv)
  Result: PASS — "69 files already formatted"
  Exit code: 0

MYPY (mypy src/ --ignore-missing-imports)
  Result: PASS — "Success: no issues found in 38 source files"
  Exit code: 0

PYTEST (pipeline modules only)
  pytest tests/test_llm_worker.py tests/test_db_repository.py tests/test_backends.py
         tests/test_orchestrator.py tests/test_perf.py tests/test_pipeline_migration.py
         tests/test_asr_worker.py tests/test_vad_worker.py -q --tb=short
  Result: 110 passed in 10.56s
  Note: Qt UI test modules (test_overlay, test_settings, test_tooltip, test_tray,
        test_learning_panel, test_pipeline) have a pre-existing QCoreApplication/QApplication
        ordering conflict when run together — they pass individually. This is a pre-existing
        test-infrastructure issue unrelated to the pipeline-perf changes.

================================================================================
SECTION 2 — ISSUES FOUND IN FIRST REVIEW AND THEIR FIXES
================================================================================

All issues from the original REJECT verdict have been resolved:

M1 [FIXED] — llm_worker.py: `llm_client` and `db_repo` now typed as concrete
  AsyncOllamaClient and LearningRepository | None (not Protocol types)

M2 [FIXED] — orchestrator.py: `connect_signals` callbacks typed as
  Callable[[ASRResult], None] and Callable[[TranslationResult], None]

M3 [FIXED] — orchestrator.py: Signal property return type comments added:
  "# PySide6 Signal has no usable stub type"

V1 [FIXED] — backends.py: soxr made optional via try/except ImportError;
  scipy.signal.resample_poly fallback implemented with gcd-based rational resampling

N1 [FIXED] — asr_worker.py: bare `pass` on queue.Empty now has comment explaining
  it is an intentional polling loop pattern

N2 [FIXED] — llm_worker.py: `# noqa: BLE001` now has inline justification:
  "# DB errors must not crash the LLM thread"

N3 [FIXED] — orchestrator.py: `import dataclasses` moved to module level

N4 [FIXED] — orchestrator.py: comment changed from "injected" to
  "created at construction time" for accuracy

N5/N6 [FIXED] — perf.py: `print()` in docstring example replaced with
  `logger.info()`; trivial `__init__`, `__enter__`, `__exit__`, `add` docstrings removed

NB-2 [FIXED] — settings.py:191: `# type: ignore[arg-type]` now has justification
  comment explaining the PySide6 enum vs string discrepancy

NB-3 [FIXED] — ollama_client.py:51: `# type: ignore[method-assign]` now has
  justification comment explaining lru_cache cache_clear attribute

Bug [FIXED] — llm_worker.py: DB call now uses `asr_result.db_row_id` (int PK)
  instead of `segment_id` (str). Guard added: `if db_row_id is not None`.

T1 [FIXED] — tests/test_pipeline_migration.py: stale test
  `test_pipeline_module_no_longer_exists` replaced with correct
  `test_pipeline_legacy_is_importable` test

================================================================================
VERDICT: APPROVE
================================================================================
All mandatory and notable issues resolved. Lint, format, and type checks clean.
110 pipeline-module tests pass. Pre-existing Qt test-isolation issue is out of scope.
