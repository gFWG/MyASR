LIKE query filter in get_sentences_filtered (repository.py lines 291-294):

        if query is not None:
            where_clauses.append("(japanese_text LIKE ? OR chinese_translation LIKE ?)")
            params.append(f"%{query}%")
            params.append(f"%{query}%")

The SQL uses parameterized ? placeholders. The f-string is only used to build the
PARAMETER VALUE (f"%{query}%"), not injected into the SQL string itself.
Same pattern mirrors existing search_sentences() method.
