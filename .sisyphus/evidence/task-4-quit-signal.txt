# Task 4 Evidence: Quit Signal Connection

## Proof that tray.quit_requested.connect(app.quit) exists in main.py

Location: /home/yuheng/MyASR-post-mvp/src/main.py

```python
# Line 115:
tray.quit_requested.connect(app.quit)
```

Full signal wiring (lines 115-118):
```python
tray.quit_requested.connect(app.quit)
tray.toggle_overlay.connect(lambda: overlay.setVisible(not overlay.isVisible()))
tray.settings_requested.connect(_open_settings)
tray.history_requested.connect(_open_learning_panel)
```

Note: tray.review_requested is NOT connected (deferred to Feature 4)
