ValueError raises for sort_by and sort_order (repository.py lines 281-286):

        _valid_sort_by = {"created_at", "japanese_text"}
        _valid_sort_order = {"ASC", "DESC"}
        if sort_by not in _valid_sort_by:
            raise ValueError(f"Invalid sort_by: {sort_by}")
        if sort_order not in _valid_sort_order:
            raise ValueError(f"Invalid sort_order: {sort_order}")

Whitelist validation runs before any SQL is built. The ORDER BY clause then uses
f"ORDER BY {sort_by} {sort_order}" safely since both values are whitelisted identifiers.
